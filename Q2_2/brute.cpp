#include <iostream>
#include <string>

using namespace std;
class	brute {
	private:
		int a;
	public:
		brute();
		int run(string lol);
};

brute::brute()
{
	
}

int brute::run(string lol)
{
	cout << lol << endl;
}